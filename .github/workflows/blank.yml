name: Ajax AI Monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ajax-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Start Ajax AI Monitor
        run: |
          echo "🔴⚪ Ajax AI Monitor gestart"
          echo "Controle uitgevoerd op $(date)"
